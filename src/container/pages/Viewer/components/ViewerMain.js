import { Typography } from 'antd';
import React from 'react';
const { Text } = Typography;
const ViewerMain = () => {
  return (
    <div className="flex bg-black w-full h-full flex-1 border-sm">
      <Text>Viewer</Text>
    </div>
  );
};

export default ViewerMain;
